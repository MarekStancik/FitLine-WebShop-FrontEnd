import { ProductsOrderbyNamePipe } from './products-orderby-name.pipe';

describe('ProductsOrderbyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsOrderbyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
